---
controller_inventories:
  - name: Localhost
    description: Localhost inventory for API-driven playbooks
    organization: Default

controller_hosts:
  - name: localhost
    inventory: Localhost
    variables:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
